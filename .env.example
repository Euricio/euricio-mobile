# Example environment for the Euricio mobile app.
# Copy to .env (gitignored) and fill in real values for local dev.
# For EAS builds, configure these as EAS secrets, not in source.
#
# ⚠️ This repo is PUBLIC — never commit real values.

EXPO_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=eyJ...anon-key...
EXPO_PUBLIC_API_URL=https://crm.euricio.es
